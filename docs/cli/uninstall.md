---
summary: "CLI reference for `ozzo uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
---

# `ozzo uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
ozzo uninstall
ozzo uninstall --all --yes
ozzo uninstall --dry-run
```

